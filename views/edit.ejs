<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div id="main" class="min-h-screen w-full bg-neutral-700 text-white p-10">

        <!-- Profile Header -->
        <div class="w-full max-w-4xl p-6">
            <div class="flex justify-between items-center">
                <h1 class="text-3xl font-semibold">Hello, Sumit Kushwah <%= %> 👋</h1>
                <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Logout</a>
            </div>
            <p class="mt-2 text-gray-400">You can edit your post.</p>
        </div>
        <!-- Post Form -->
        <div class="w-full max-w-4xl bg-gray-800 p-6 rounded-lg shadow-lg mb-6">
            <form action="/update/<%= post._id%>" method="post" class="space-y-4">
                <textarea name="postdata"
                    class="w-full p-4 text-black rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                    placeholder="What's on your mind?" rows="3"><%= post.postdata %></textarea>
                <button type="submit" class="bg-yellow-500/85 text-white px-6 py-2 rounded hover:bg-yellow-600 capitalize">update post</button>
            </form>
        </div>
    </div>

</body>

</html>